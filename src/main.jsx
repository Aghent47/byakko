import React from 'react';
import ReactDOM from 'react-dom/client';
import { AppByakko } from './components/AppByakko';
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AppByakko />
  </React.StrictMode>,
)
